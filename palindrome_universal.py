import time
from math import floor

digits_list1 = [1, 0, 2, 4]
digits_list2 = [5]
digits_list3 = [9, 3, 9]
digits_list3 = [1, 9, 2, 5, 7, 7, 5, 2, 9, 1]

def check_is_palindrome(digits_list: list):
    check_number = floor(len(digits_list)/2)
    current_pochatok = digits_list[0] 
    current_kinetc = len(digits_list) - 1
    current_
digits_list = ('8,4,5,8'.split(","))
check_is_palindrome(digits_list)
